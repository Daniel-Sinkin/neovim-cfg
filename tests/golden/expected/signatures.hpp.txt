namespace lldb { struct SBEvent {}; }
class Foo
{
    static bool EventIsTargetEvent(const lldb::SBEvent &event);
    SBValue ^GetValue(int idx) const;
    void run() mut $ne;
    auto trailing() const -> int;
};
bool freeFunc(int x);

class Bar
{
    $nd auto x() const $ne    -> int;
    $nd auto name() const $ne -> CString;
    auto reset(int n) mut $ne -> mut Bar&;
};

class Ops
{
    auto operator()(window^ window) const $ne -> void;
    auto operator[](int i) mut $ne            -> mut int&;
    Ops& operator=(const Ops& other) mut $ne;
    auto operator==(const Ops& o) const $ne -> bool;
};

// clang-format off
auto kept_raw(int a,    int b) -> int;
auto and_this  (float x)       -> float;
// clang-format on
