namespace lldb { struct SBEvent {}; }
class Foo
{
    bool EventIsTargetEvent(event: const lldb::SBEvent &);
    SBValue ^GetValue(idx: int) const;
    void run() mut $ne;
    auto trailing() const -> int;
};
bool freeFunc(x: int);

class Bar
{
    $nd auto x() const $ne     -> int;
    $nd auto name() const $ne  -> CString;
    auto reset(n: int) mut $ne -> mut Bar&;
};

class Ops
{
    auto operator()(window: window^) const $ne -> void;
    auto operator[](i: int) mut $ne            -> mut int&;
    Ops& operator=(other: const Ops&) mut $ne;
    auto operator==(o: const Ops&) const $ne -> bool;
};

// clang-format off
auto kept_raw(int a,    int b) -> int;
auto and_this  (float x)       -> float;
// clang-format on
