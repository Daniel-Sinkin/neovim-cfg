namespace test
{
<V>
concept StringLike = V ~> string_view;
<R>
concept StringRange = R~input_range and (R~value ~> string_view);
<T>
concept IsInt = T ~= int;
<F>
concept Callback = F(int);
<T>
concept CharCol = T~& ~> char;
<T>
concept BoolCol = (T ~> bool) and (T ~> int);
<I>
concept ItChar = I~value ~= char;
<R>
concept RefBool = R~& ~> bool;
<Proj, R>
concept Proj1 = Proj&(R~&) ~> string_view;

static_assert(std::same_as<ValueOf<R>, char>);
static_assert(std::same_as<std::underlying_type_t<VkResult>, int>);
}  // namespace dans::test
